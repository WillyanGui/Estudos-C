#include<stdio.h>
int main()
{
    float n1, n2 , soma , divisao , mult, vx, resultado;
    char operador;
    printf("Insira um operador = \n");
    scanf("%s", &operador);
    

    switch (operador)
    {
    case 1:
        if (operador==)
        {
            resultado= n1+n2;
            printf("Resultado", resultado);
        }
        break;
    
    default:
        break;
    }


    return 0;
}
